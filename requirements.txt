# Спайк turn detection — инференс обеих моделей (torch не нужен, всё через ONNX)

# --- модели и инференс ---
numpy==2.4.6
onnxruntime==1.27.0
transformers==5.12.1
huggingface_hub==1.21.0
jinja2==3.1.6            # chat template для LiveKit-модели

# --- аудио (Smart Turn) ---
librosa==0.11.0
soundfile==0.14.0

# --- датасеты и метрики ---
pyarrow==24.0.0         # чтение parquet-шардов smart-turn-data
pandas==3.0.3           # таблицы результатов
scikit-learn==1.9.0     # ROC-AUC, confusion matrix, precision/recall

# --- ноутбуки ---
jupyter==1.1.1
ipykernel==7.3.0
nbformat==5.10.4

# --- опционально: генерация тестовой русской речи через gTTS ---
gtts==2.5.4
